#ifndef SEQUENCES_HPP
#define SEQUENCES_HPP

namespace Sequences {
    using sequence_index = int;

    sequence_index TURN_ON_SEQUENCE = 1;
    sequence_index LEFT_TURN_ON_SQEUENCE = 2;
    sequence_index RIGHT_TURN_ON_SQEUENCE = 3;
    sequence_index TURN_OFF_SEQUENCE = 4;
};

#endif // !SEQUENCES_HPP